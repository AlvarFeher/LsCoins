{% extends "base.twig" %}

{% block title %}Sign-up | LSCoins{% endblock %}

{% block content %}
    <h1>Sign up here!</h1>
    <form data-cy="sign-up" action="{{ formAction }}" method="POST">

        <label> Email </label>
        <input data-cy="sign-up__email" type="text" name="email" placeholder="Enter your email"
               value="{{ formData.email }}">
        {% if formErrors['email'] %}
            <span data-cy="sign-up__wrongEmail">{{ formErrors['email'] }}</span>
        {% endif %}

        <label> Password </label>
        <input data-cy="sign-up__password" type="password" name="password" placeholder="Enter your password">

        <label> Repeat Password </label>
        <input data-cy="sign-up__repeatPassword" type="password" name="repeatPassword"
               placeholder="Repeat your password">
        {% if formErrors['password'] %}
            <span data-cy="sign-up__wrongPassword">{{ formErrors['password'] }}</span>
        {% endif %}

        <label> LSCoins to deposit </label>
        <input data-cy="sign-up__coins" type="int" name="coins"
               value="{{ formData.coins }}"/>
        {% if formErrors['coins'] %}
            <span data-cy="sign-up__wrongCoins">{{ formErrors['coins'] }}</span>
        {% endif %}

        <input data-cy="sign-up__btn" type="submit" value="Sign-up">
    </form>
{% endblock %}
